{% extends 'back/base.html.twig' %}

{% block body %}

    {% include 'back/leftPanel.html.twig' %}


    <!-- Right Panel -->

    <div id="right-panel" class="right-panel">

        {% include 'back/header.html.twig' %}

        <div class="breadcrumbs">
            <div class="col-sm-4">
                <div class="page-header float-left">
                    <div class="page-title">
                        <h1>Pozycje modułu</h1>
                    </div>
                </div>
            </div>
            <div class="col-sm-8">
                <div class="page-header float-right">
                    <div class="page-title">
                        <ol class="breadcrumb text-right">
                            <li class="active">Pozycje modułu</li>
                        </ol>
                        <div class="page-header float-right">
                            <a href="{{ path('admin_module_position_new') }}"><i class="fa fa-user-plus text-dark"></i></a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        {% include 'back/flashMessages.html.twig' %}

        <div class="content mt-3">
            <div class="animated fade-in">
                <table class="table">
                    <thead class="thead-dark">
                        <tr>
                            <th>Id</th>
                            <th>Nazwa</th>
                            <th>Pozycja</th>
                            <th>Usuń</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for modulePosition in modulePositions %}
                            <tr>
                                <td>{{ modulePosition.id }}</td>
                                <td><a href="{{ path('admin_module_position_edit',{ 'id':modulePosition.id }) }}">{{ modulePosition.name }}</a></td>
                                <td>{{ modulePosition.name }}</td>
                                <td><a href="{{ path('admin_module_position_delete', { 'id':modulePosition.id }) }}" class="btn btn-danger">Usuń</a></td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
        </div>
        <!--/.col-->

{% endblock %}

