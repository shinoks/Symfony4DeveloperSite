{% extends 'back/base.html.twig' %}

{% block body %}

    {% include 'back/leftPanel.html.twig' %}

    <!-- Right Panel -->

    <div id="right-panel" class="right-panel">

    {% include 'back/header.html.twig' %}

    <div class="breadcrumbs">
        <div class="col-sm-4">
            <div class="page-header float-left">
                <div class="page-title">
                    <h1>{{ 'recruitment_user'|trans }}</h1>
                </div>
            </div>
        </div>
        <div class="col-sm-8">
            <div class="page-header float-right">
                <div class="page-title">
                    <ol class="breadcrumb text-right">
                        <li class="active"><a href="{{ path('admin_recruitment_users') }}">{{ 'recruitment_user'|trans }}</a> > {{ 'edit_recruitment_user'|trans }}</li>
                    </ol>
                </div>
            </div>
        </div>
    </div>

        {% include 'back/flashMessages.html.twig' %}

    <div class="card">
        <div class="card-body">
            <div class="card card-body">
                <h5>Użytkownik:</h5>
                <a href="{{ path('admin_user_edit',{'id':recruitmentUser.user.id}) }}">{{ recruitmentUser.user.firstName }} {{ recruitmentUser.user.lastName }}</a><br/>
            </div>
            <div class="card card-body">
                <h5>Dane oferty</h5>
                {% form_theme form 'form/default_admin.html.twig' %}

                {{ form_start(form) }}
                {{ form_widget(form) }}

                {{ form_end(form) }}
            </div>
        </div>
    </div>
    </div>
{% endblock %}
